//some functions i wrote but didn't use. Maybe will use later

// const searchFunc = () => {
//     const replace = (str: string) => str.toLowerCase().replace(/[\s.\-\:,%]/gi, '');
//     let newData = [];
//     for (let i = 0; i < listData.length; i++) {
//         if (replace(listData[i].title).includes(replace(searchedMovie)) || replace(listData[i].original_title).includes(replace(searchedMovie))) {
//             newData.push(listData[i])
//         }
//     }
//     return newData
// };

export {}